require "spec_helper"

module RansackAbbreviator
  describe "parsing a config.yml" do
    context "when the file exists" do
      it "initializes the abbreviations"
    end
    
    context "when the file is missing" do
      it "does nothing" do
        
      end
    end
  end
  
  describe "messing with column shortcuts" do
    it "adds column shortcuts"
  
    it "removes column shortcuts"
    
    it "modifies column shortcuts"
    
    it "allows columns to have duplicate shortcuts"
  end  
end